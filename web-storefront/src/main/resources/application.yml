server:
  port: 8090

spring:
  application:
    name: web-storefront
  thymeleaf:
    cache: false

gateway:
  url: ${GATEWAY_URL:http://localhost:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
