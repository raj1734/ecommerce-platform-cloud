spring:
  application:
    name: catalog-service
  profiles:
    active: ${CATALOG_REPOSITORY_TYPE:memory},cache-${CATALOG_CACHE_TYPE:memory}
  config:
    import: optional:configserver:http://${CONFIG_SERVER_HOST:localhost}:8889
